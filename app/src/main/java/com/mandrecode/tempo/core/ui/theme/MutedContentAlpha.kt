package com.mandrecode.tempo.core.ui.theme

/**
 * Alpha values for muting card title/description text once an item (task or habit) is marked
 * completed. Shared between tasks and routines so the completed-state treatment stays identical
 * across both surfaces.
 */
object MutedContentAlpha {
    const val TITLE = 0.6f
    const val DESCRIPTION = 0.4f
}
