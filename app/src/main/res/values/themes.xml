<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Tempo" parent="android:Theme.Material.Light.NoActionBar" />

    <style name="Theme.Tempo.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@android:color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Tempo</item>
    </style>

    <!-- Transparent trampoline theme for QuickAddTaskActivity, launched from the home-screen
         widget: shows only the Compose dialog surface with a dimmed scrim behind it. -->
    <style name="Theme.Tempo.QuickAddTask" parent="Theme.Tempo">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>
</resources>
