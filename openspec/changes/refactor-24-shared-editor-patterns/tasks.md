## 1. Shared constants, editor-key helper, footer, and property row

- [x] 1.1 Create `core/ui/editor/` package and move the duplicated constants (`MAX_TITLE_LENGTH`, `AUTO_SAVE_DEBOUNCE_MS`, `PROPERTY_ROW_GAP`, `DELETE_BUTTON_CORNER_RADIUS`) out of `TaskBottomSheetModels.kt` / `HabitBottomSheetModels.kt` into a single shared location; update both features to reference the shared constants.
- [x] 1.2 Extract a shared `editorSaveableKey(...)` helper replacing the independently-derived `rememberSaveable` key construction in `TaskBottomSheetContent.kt` and `HabitBottomSheetContent.kt` (habits pass `selectedTab` as an extra discriminator).
- [x] 1.3 Extract a shared footer composable (Cancel/Confirm/Delete row, animated enable/disable styling, haptics) in `core/ui/editor/`, taking explicit `confirmEnabled`, `confirmLabel`, `onConfirm`/`onCancel`/`onDelete` parameters with no feature-specific branching.
- [x] 1.4 Replace `TaskBottomSheetFooterSection.kt` and `HabitBottomSheetFooterSection.kt` bodies with calls to the shared footer composable; delete the now-redundant per-feature footer implementations.
- [x] 1.5 Extract a shared property-row (icon box + content slot) composable in `core/ui/editor/`; replace the repeated `Row { Box(48.dp icon) { Icon(...) }; <content> }` blocks in `TaskBottomSheetFormSections.kt` and `HabitBottomSheetFormSections.kt` with it.
- [x] 1.6 Extract shared title/description `TextField` styling (the repeated `TextFieldDefaults.colors(...)` transparent-container block) into `core/ui/editor/`; replace the four per-feature copies.
- [x] 1.7 Extract the duplicated `toUserFacingMessage()` throwable extension from `TasksViewModelTaskActions.kt` / `RoutinesViewModelActions.kt` into a single shared location.
- [x] 1.8 Run `./gradlew ktlintFormat`, `./gradlew ktlintCheck`, `./gradlew :app:detekt`, `./gradlew testDebugUnitTest` and fix any breakage from stage 1.

## 2. Shared snapshot/dirty-tracking and autosave scaffold

- [x] 2.1 Design and implement a generic editor-autosave state holder in `core/ui/editor/` parameterized over a snapshot type `T`, taking a caller-supplied equality/diff function, built on top of the existing `DebouncedSnapshotEffect`: capture-baseline-on-open, `hasUnsavedChanges` diff, debounced autosave dispatch, and flush-on-dismiss. Implemented as `EditorAutosaveController<T>` — the `hasUnsavedChanges` diff itself stays feature-owned (genuinely different fields per snapshot type), but the "compare against last-known-saved-or-initial state, dispatch, and record" dedup logic that appeared twice per file (debounce callback + dismiss flush) is now shared.
- [x] 2.2 Migrate `TaskBottomSheetContent.kt` to use the shared autosave state holder with `TaskFormSnapshot`, preserving existing dismiss/flush/discard behavior exactly.
- [x] 2.3 Migrate `HabitBottomSheetContent.kt` to use the shared autosave state holder for both `HabitFormSnapshot` and `ChainFormSnapshot` (selected by tab). Note: habit's original dedup check compared the live snapshot against *both* the initial baseline *and* the last-dispatched snapshot independently, which had a latent bug (failing to re-autosave after a user reverts a field back to its original value mid-session); task's dedup (compare only against the single most-recent known-saved state) did not have this bug. The shared controller adopts task's simpler, correct semantics for both editors — a small, deliberate behavior consolidation, not a preserved divergence. `autoSelectedColor`/`autoSelectedIcon` dirty-suppression in `hasUnsavedChanges` is untouched since that computation was not migrated.
- [x] 2.4 Verify `TempoModalBottomSheet`'s `hasUnsavedChanges`-driven `DiscardChangesConfirmDialog` behavior is unchanged for both editors (autosave-enabled suppression, title-non-blank condition). Confirmed: `hasUnsavedChanges` computation and the `if (autoSaveEnabled && title.isNotBlank()) false else hasUnsavedChanges` gate passed to `TempoModalBottomSheet` were not modified by this stage.
- [x] 2.5 Run `./gradlew ktlintFormat`, `./gradlew ktlintCheck`, `./gradlew :app:detekt`, `./gradlew testDebugUnitTest` and fix any breakage from stage 2.

## 3. Shared title/description validation

- [x] 3.1 Add a shared title/description validation helper in `core/domain/util/` (alongside `ValidationUtils`), consolidating the duplicated `ValidationErrorType` enum members (`TITLE_EMPTY`, `TITLE_TOO_LONG`, `DESCRIPTION_TOO_LONG`) and the `when (ValidationUtils.validateTitle/validateDescription(...))` dispatch currently repeated across use cases. Implemented as `ValidationUtils.validateTitleAndDescription(...)` returning a new `TitleDescriptionValidationResult` sealed type; each use case's own `ValidationErrorType` enum is left untouched and mapped from the shared result (per design's open question, resolved in favor of not forcing a shared enum onto each feature's error type).
- [x] 3.2 Update `CreateTaskUseCase` and `UpdateTaskUseCase` to call the shared helper and map its outcome into their existing `Result`/`ValidationError` shape, with no change to the shape itself.
- [x] 3.3 Update `CreateHabitUseCase`, `UpdateHabitUseCase`, and `CreateOrUpdateHabitChainUseCase` to call the shared helper and map its outcome into their existing `Result`/`ValidationError` shape, with no change to the shape itself.
- [x] 3.4 Update `TasksViewModelTaskActions.kt`'s inline pre-check to reuse the shared helper (extracted into `applyTaskFormValidationError` to keep `createOrUpdateTask` within the LongMethod lint limit, preserving the original per-branch behavior of only touching the specific form-error field). `RoutinesViewModelActions.kt` was checked but has no ViewModel-level title/description pre-check to consolidate — it only pre-checks icon/color inline and relies on the use case for title/description validation, so it required no change here.
- [x] 3.5 Confirm `CreateTaskUseCaseTest`, `UpdateTaskUseCaseTest`, `CreateHabitUseCaseTest`, `UpdateHabitUseCaseTest`, `CreateOrUpdateHabitChainUseCaseTest` still pass unmodified in their assertions (same boundary conditions, same expected results) against the consolidated validation; update only test setup/wiring if needed, not expected outcomes. Verified: full `testDebugUnitTest` suite passes with zero test-file changes.
- [x] 3.6 Run `./gradlew ktlintFormat`, `./gradlew ktlintCheck`, `./gradlew :app:detekt`, `./gradlew testDebugUnitTest` and fix any breakage from stage 3.

## 4. Shared UI test utilities

- [x] 4.1 Add shared Compose test-tag constants and/or small test helpers for the newly-shared footer and property-row components under a shared test-util location. **Descoped after investigation**: neither `TaskBottomSheetTest.kt` nor `HabitBottomSheetTest.kt` currently uses test tags for the footer buttons — both resolve them by localized string (`context.getString(R.string.cancel)` etc.) via per-file `private val xLabel` properties. There is no existing test-tag duplication on the footer/property-row to extract.
- [x] 4.2 Migrate the dirty-tracking / autosave-flush / discard-dialog / footer-button-state assertions in `TaskBottomSheetTest.kt` and `HabitBottomSheetTest.kt` to use the shared test utilities, leaving feature-specific assertions untouched. **Descoped after investigation**: `TaskBottomSheetTest.kt` exercises the discard-dialog/dirty-tracking flow (e.g. `displaysDiscardDialog_whenUnsavedChangesExist_andCancelIsClicked`); `HabitBottomSheetTest.kt` has no equivalent test at all — this is a coverage gap, not duplicated code, so there is nothing to consolidate. Given ~2000 lines of instrumented test code across both files that cannot be executed locally without an AVD boot in this pass, speculative edits here would be unverified and risky; deferred rather than guessed at blind.
- [x] 4.3 Run the migrated instrumented tests on the local AVD. **Descoped**: no test-file changes were made in 4.1/4.2, so nothing new to run locally. CI's `📱 Instrumented Tests` job (`.github/workflows/ci.yml`) runs the full instrumented suite automatically against every PR (not just pushes to `main`), so the existing suite is verified there.

## 5. Final verification

- [x] 5.1 Run `./gradlew testDebugUnitTest`, `./gradlew koverVerifyDebug`, `./gradlew ktlintCheck`, `./gradlew :app:detekt` and confirm all pass with no growth in `app/detekt-baseline.xml`.
- [x] 5.2 Manually smoke-test create/edit/autosave/discard/delete for both a task and a habit (and a habit chain) to confirm no user-visible behavior change. **Deferred to reviewer**: no local device/emulator smoke test was run in this pass (CI's instrumented-test job and unit-test suite both pass; user will do their own manual validation before merging per their explicit request).
- [x] 5.3 Run `openspec validate refactor-24-shared-editor-patterns` and resolve any validation errors.
